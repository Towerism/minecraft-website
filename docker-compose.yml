version: "3"
services:
  minecraft-website:
    container_name: minecraft-website
    image: martinfjr/minecraft-website:latest
    pull_policy: always
    restart: unless-stopped
    networks:
      - web-proxy-net

networks:
  web-proxy-net:
    external: true